<?xml version="1.0" encoding="utf-8"?>
<!-- shown when translation is switched on with no key saved yet; the key it
     takes is the same prefs["apiKey"] the Settings screen edits -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="22dp"
    android:paddingEnd="22dp"
    android:paddingTop="8dp"
    android:paddingBottom="4dp">

    <EditText
        android:id="@+id/dialogApiKey"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_input"
        android:padding="11dp"
        android:hint="sk-ant-…"
        android:textSize="14sp"
        android:textColor="@color/fg"
        android:textColorHint="@color/muted"
        android:inputType="textPassword"
        android:importantForAutofill="no" />

    <TextView
        android:id="@+id/dialogApiKeyLink"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:text="Where do I get a key?"
        android:textSize="13sp"
        android:textColor="@color/accent"
        android:clickable="true"
        android:focusable="true" />
</LinearLayout>
